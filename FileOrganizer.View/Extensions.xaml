<UserControl x:Class="FileOrganizer.View.Extensions"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FileOrganizer.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <ListView ItemsSource="{Binding Path=LoadedExtensions}" HorizontalAlignment="Center">
            <ListView.View>
                <GridView>
                    <!--<GridViewColumn Header="Id" Width="120" DisplayMemberBinding="{Binding Id}" />-->
                    <GridViewColumn Header="Name" Width="50" DisplayMemberBinding="{Binding ExtensionName}"/>
                </GridView>
            </ListView.View>
        </ListView>
            <TextBlock Text="Extensions" HorizontalAlignment="Center" VerticalAlignment="Center"></TextBlock>
    </Grid>
</UserControl>
